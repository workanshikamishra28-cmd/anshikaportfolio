# 🏗️ Portfolio Architecture with Sanity CMS

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    ANSHIKA PORTFOLIO                         │
│                  (React + Vite + TypeScript)                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ Fetches Data
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      SANITY CMS                              │
│                   (Content Management)                       │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │    Hero      │  │   Projects   │  │   Resume     │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │    About     │  │ Selected     │  │   Contact    │     │
│  │              │  │   Works      │  │              │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ Manages Content
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    SANITY STUDIO                             │
│                  (localhost:3333)                            │
│                                                              │
│  • Visual Editor                                            │
│  • Image Upload                                             │
│  • Content Management                                       │
│  • Custom Theme                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Data Flow

```
┌──────────────┐
│   Browser    │
│              │
│  Portfolio   │
│  (React App) │
└──────┬───────┘
       │
       │ 1. Component Mounts
       │
       ▼
┌──────────────────────┐
│  React Component     │
│                      │
│  useEffect(() => {   │
│    fetchData()       │
│  }, [])              │
└──────┬───────────────┘
       │
       │ 2. Calls Query Function
       │
       ▼
┌──────────────────────┐
│  sanity-queries.ts   │
│                      │
│  getProjects()       │
│  getHeroData()       │
│  getAboutData()      │
└──────┬───────────────┘
       │
       │ 3. Uses Sanity Client
       │
       ▼
┌──────────────────────┐
│    sanity.ts         │
│                      │
│  createClient({      │
│    projectId,        │
│    dataset           │
│  })                  │
└──────┬───────────────┘
       │
       │ 4. Fetches from API
       │
       ▼
┌──────────────────────┐
│   Sanity Cloud       │
│                      │
│  • Content Storage   │
│  • Image CDN         │
│  • API Endpoint      │
└──────┬───────────────┘
       │
       │ 5. Returns Data
       │
       ▼
┌──────────────────────┐
│  React Component     │
│                      │
│  setData(result)     │
│  Renders UI          │
└──────────────────────┘
```

---

## Component Architecture

```
src/
│
├── components/
│   │
│   ├── Hero.tsx
│   │   └─→ getHeroData() ──┐
│   │                        │
│   ├── About.tsx            │
│   │   └─→ getAboutData() ─┤
│   │                        │
│   ├── Projects.tsx ✅      │
│   │   └─→ getProjects() ──┤
│   │                        │
│   ├── SelectedWorks.tsx    │
│   │   └─→ getSelectedWorks()
│   │                        │
│   ├── Resume.tsx           │
│   │   └─→ getResumeData() ┤
│   │                        │
│   └── Contact.tsx          │
│       └─→ getContactData() ┤
│                            │
├── lib/                     │
│   │                        │
│   ├── sanity-queries.ts ◄──┘
│   │   │
│   │   └─→ Uses client from sanity.ts
│   │
│   └── sanity.ts
│       │
│       └─→ Connects to Sanity Cloud
│
└── types/
    │
    └── sanity.ts
        │
        └─→ TypeScript definitions for all content
```

---

## Content Schema Structure

```
Sanity Studio
│
├── Hero (Single Document)
│   ├── experienceBadge: string
│   ├── portfolioTitle: { line1, line2 }
│   ├── subtitle: { line1, line2 }
│   ├── metadata: { copyright, year }
│   └── ctaButton: { text, icon }
│
├── About (Single Document)
│   ├── heading: { line1, line2, line3 }
│   ├── description: text
│   ├── profileImage: image
│   ├── designPhilosophy: { title, content }
│   └── marqueeText: string
│
├── Projects (Multiple Documents)
│   ├── title: string
│   ├── slug: slug
│   ├── category: select
│   ├── image: image
│   ├── description: text
│   ├── order: number
│   └── featured: boolean
│
├── Selected Works (Multiple Documents)
│   ├── title: string
│   ├── slug: slug
│   ├── description: text
│   ├── category: string
│   ├── image: image
│   ├── backgroundColor: string
│   └── order: number
│
├── Resume (Single Document)
│   ├── name: string
│   ├── title: string
│   ├── bio: text
│   ├── profileImage: image
│   ├── experience: array
│   ├── education: object
│   ├── skills: { design[], tools[] }
│   └── socialLinks: object
│
└── Contact (Single Document)
    ├── heading: string
    ├── subheading: text
    ├── email: string
    ├── phone: string
    ├── location: string
    └── socialMedia: object
```

---

## Technology Stack

```
┌─────────────────────────────────────────┐
│           FRONTEND LAYER                │
├─────────────────────────────────────────┤
│  • React 18                             │
│  • TypeScript                           │
│  • Vite                                 │
│  • Tailwind CSS                         │
│  • React Router                         │
└─────────────────────────────────────────┘
                  │
                  │ Fetches Data
                  ▼
┌─────────────────────────────────────────┐
│         CONTENT LAYER (CMS)             │
├─────────────────────────────────────────┤
│  • Sanity CMS                           │
│  • @sanity/client                       │
│  • @sanity/image-url                    │
│  • Content API                          │
└─────────────────────────────────────────┘
                  │
                  │ Stores Content
                  ▼
┌─────────────────────────────────────────┐
│          BACKEND LAYER                  │
├─────────────────────────────────────────┤
│  • Sanity Cloud                         │
│  • Image CDN                            │
│  • Content Lake                         │
│  • Real-time API                        │
└─────────────────────────────────────────┘
```

---

## Design System Integration

```
Portfolio Design System
│
├── Colors
│   ├── Primary: #78201B (Persian Plum)
│   ├── Background: #F8EDEA (Off-White)
│   └── Accent: #D4A574 (Gold)
│
├── Typography
│   ├── Headings: Playfair Display
│   └── Body: Inter
│
├── Animations
│   ├── Floating elements
│   ├── Marquee scrolling
│   ├── Stacking cards
│   └── Smooth transitions
│
└── Effects
    ├── Noise textures
    ├── Dithered patterns
    ├── Gradient overlays
    └── Backdrop blur

    ↓ Applied to ↓

Sanity Studio Theme
│
├── Custom Colors
│   ├── Base BG: #F8EDEA
│   ├── Base FG: #78201B
│   └── Border: #D4A574
│
└── Button Styles
    ├── Default: #78201B
    └── Primary: #78201B
```

---

## Query Pattern

```typescript
// 1. Define Type
interface Project {
  _id: string
  title: string
  category: string
  image?: SanityImage
}

// 2. Create Query Function
export async function getProjects() {
  const query = `*[_type == "project"]{
    _id,
    title,
    category,
    image{
      asset->{_id, url},
      alt
    }
  }`
  return await client.fetch(query)
}

// 3. Use in Component
const [projects, setProjects] = useState<Project[]>([])

useEffect(() => {
  getProjects().then(setProjects)
}, [])

// 4. Render
{projects.map(project => (
  <div key={project._id}>
    <h3>{project.title}</h3>
    {project.image && (
      <img src={project.image.asset.url} />
    )}
  </div>
))}
```

---

## Image Optimization Flow

```
┌──────────────────┐
│  Upload Image    │
│  in Studio       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Sanity Cloud    │
│  Processes Image │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Image CDN       │
│  • Resize        │
│  • Optimize      │
│  • Cache         │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  urlFor() Helper │
│  .width(800)     │
│  .height(600)    │
│  .url()          │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  <img> in        │
│  Component       │
└──────────────────┘
```

---

## Development Workflow

```
┌─────────────────────────────────────────┐
│  1. Start Development                   │
│     npm run dev (Portfolio)             │
│     npm run studio (CMS)                │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  2. Add/Edit Content                    │
│     • Open Studio (localhost:3333)      │
│     • Create/Edit documents             │
│     • Upload images                     │
│     • Publish changes                   │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  3. View Changes                        │
│     • Refresh portfolio                 │
│     • See updated content               │
│     • Test functionality                │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  4. Deploy                              │
│     • Build: npm run build              │
│     • Deploy Studio: npm run            │
│       studio:deploy                     │
└─────────────────────────────────────────┘
```

---

## Security & Performance

### Security
```
┌─────────────────────────────────────────┐
│  Public Read Access                     │
│  • Portfolio fetches data publicly      │
│  • No authentication needed for reads   │
│  • CDN enabled for fast delivery        │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  Authenticated Write Access             │
│  • Studio requires login                │
│  • Only authorized users can edit       │
│  • Version control & history            │
└─────────────────────────────────────────┘
```

### Performance
```
┌─────────────────────────────────────────┐
│  Content Delivery                       │
│  • CDN for images                       │
│  • Cached responses                     │
│  • Optimized queries                    │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  Frontend Optimization                  │
│  • React lazy loading                   │
│  • Image lazy loading                   │
│  • Efficient re-renders                 │
└─────────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────┐
│         PRODUCTION SETUP                │
└─────────────────────────────────────────┘

Portfolio (Frontend)
├── Hosted on: Vercel/Netlify/etc.
├── Build: npm run build
└── Fetches from: Sanity Cloud API

Sanity Studio (CMS)
├── Hosted on: your-project.sanity.studio
├── Deploy: npm run studio:deploy
└── Manages: Content in Sanity Cloud

Sanity Cloud (Backend)
├── Content Storage
├── Image CDN
├── API Endpoints
└── Real-time Updates
```

---

## Migration Path

```
Current State:
┌──────────────────┐
│  Component       │
│  with hardcoded  │
│  data            │
└──────────────────┘

         ↓ Migrate ↓

Target State:
┌──────────────────┐
│  Component       │
│  fetches from    │
│  Sanity          │
└──────────────────┘

Status:
✅ Projects      (Migrated)
⏳ Hero          (Example provided)
⏳ About         (To migrate)
⏳ Selected Works (To migrate)
⏳ Resume        (To migrate)
⏳ Contact       (To migrate)
```

---

## File Organization

```
ansika-pixel-perfect-main/
│
├── sanity-studio/           # CMS Backend
│   ├── schemaTypes/        # Content models
│   ├── sanity.config.ts    # Studio config
│   └── seed-data.ts        # Sample data
│
├── src/
│   ├── components/         # React components
│   │   ├── Projects.tsx    # ✅ Uses Sanity
│   │   └── ...             # ⏳ To migrate
│   │
│   ├── lib/               # Utilities
│   │   ├── sanity.ts      # Client setup
│   │   └── sanity-queries.ts # Data fetching
│   │
│   └── types/             # TypeScript
│       └── sanity.ts      # Content types
│
└── Documentation/          # Guides
    ├── QUICKSTART_SANITY.md
    ├── SANITY_INTEGRATION.md
    ├── CONTENT_STRUCTURE.md
    ├── MIGRATION_CHECKLIST.md
    └── ARCHITECTURE.md     # This file
```

---

This architecture provides:
- ✅ Separation of concerns
- ✅ Type safety
- ✅ Scalability
- ✅ Maintainability
- ✅ Performance
- ✅ Security

**Built for growth and ease of use!** 🚀
